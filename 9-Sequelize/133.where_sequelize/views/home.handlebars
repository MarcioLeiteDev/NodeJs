<h1>Usuarios Cadastrados</h1>

<div class="container">
    {{#each users}}

    <div class="item">
        <h2>
           Nome: {{this.name}} 
        </h2>
        <p>
            Profissão: {{this.occupation}}
        </p>
            <div class="actions">
        <a href="/users/{{this.id}}">Ver detalhes</a>
    </div>
    </div>


    {{/each}}
</div>

